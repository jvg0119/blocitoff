<% if @item.destroyed? %>
  //$('#item-<%#= @item.id %>').hide(); // this style works also
  $('#item-' + <%= @item.id %>).fadeOut(500);
<% else %>
   $('#item-' +<%= @item.id %>).append("<div class='alert alert-danger'><%= flash[:error] %></div>");
<% end %>

